# 2026-09-13 リサーチダイジェスト

> **前日の最優先行動は実行されたか: NO。**
> 根拠: `decisions/` は README と template のみで**ファイルは 0 件。**`git log -1 -- topics.yaml fetch_candidates.py` は **`313a283` (2026-07-03)** のまま (fetch 修正、**未実施 21 日目**)。`~/w/ML_report` の最新コミットは **2026-08-14 (bf0a741)** で **30 日間変化なし。****11 日連続の NO。**

---

## 本日は W37 の最終日。09-11 に立てた仮説の判定を下す —— **反証された**

09-11 に「**助言ではなく、そのまま貼れる文を先に置くと動くか**」という仮説を立て、判定条件を**「W37 の週次レビューの時点で `decisions/` にファイルが増えたか」**と決めた。**W37 (9/7–9/13) は本日で終わる。`decisions/` のファイルは 0 件。**

**判定: NO。仮説は反証された。**3 日間 (09-11 / 09-12 / 09-13)、同一のテンプレートを DIGEST 冒頭に置き続けたが、貼られなかった。

これで**同じ結論に達した仮説が 3 つになった:**

| 週 | 仮説 | 判定 |
|---|---|---|
| W34 | 作業を**軽くすれば**動く | 反証 |
| W35 | **価値で選べば**動く | 反証 |
| **W37** | **そのまま貼れる文**を置けば動く | **反証 (本日)** |

**3 つの仮説はすべて「DIGEST の書き方を変える」型だった。共通して効かなかった、というのが W37 の結論である。**次の仮説を立てるなら、**DIGEST の外側**に置くしかない (例: cron で `decisions/` に空ファイルを作らせる、`git commit` を gate にする等)。**本日はそれを設計せず、事実として記録するだけにする。**W38 の週次レビューで扱うこと。

**したがって以下の「貼るもの」は、今日から形を変える。**テンプレートを貼るのではなく、**`git diff` が残る 1 行の変更**を 2 つ置く。効かなければ、それも記録になる。

---

## 貼るもの ① — `topics.yaml` の 2 行を書き換える (所要 1 分)

**本日、修正① を「4 回目の実測で妥当らしい」から「曜日で決まっている」に格上げできた。**過去 6 週間の全実行を fetch した曜日で並べ直すと、**土・日・月に実行した 13 回すべてで本流 3 トピックの候補が 0 件、反例なし。**火〜金の 17 回はすべて 38〜54 件 (詳細は [rejected.md](rejected.md) §0)。

原因は 1 行で言える。**arXiv API の `published` は投稿時刻だが、API に論文が現れるのは announce 後で、その遅れが 1〜4 日ある。**`fetch_candidates.py` は投稿時刻に対して窓を切るので、**`lookback_days: 2` の 2 日のうち手前の 1〜2 日は最初から空である。**土曜実行では窓の幅が実質ゼロになる。

**これは「fm_distill が 3 日連続で 30 件の天井に張り付く」「next_arch の最古が cutoff の 2 分後」「① の 2 つは独立でない」を、すべて 1 つの原因で説明する。**窓が実質 1 announce 日分しかないから、その 1 日で 30 件を超え、論文は窓の下端に詰まる。

`topics.yaml` の `global:` を下のとおりにする。**2 行を同時に変えること** —— `lookback_days` だけ上げると 2 日目の分が上位 30 件の切り捨てで消え、`max_results` だけ上げても窓が届かない。

```yaml
global:
  max_deep_per_day: 6
  lookback_days: 5           # 2 → 5
```

`max_results` は `fetch_candidates.py` の 1 箇所:

```python
def query_arxiv(categories, keywords, max_results=100):   # 30 → 100
```

> **本日分かった注意点を 1 つ。土日月は、これを入れても「新しい論文」は増えない。**lb=5 にすれば本日の候補は 0 → 約 41 件に戻るが、**その 41 件は昨日読んだのと同じ論文である** (arXiv に新しい announce が無いのだから当然)。**dedup (修正②) が入るまでは、土日月に「既読 40 件が毎日再配信される」状態になる。**①と②は、土日月については切り離せない。

## 貼るもの ② — この loop が自分で書き残した 6 件を回収する (所要 3 分)

**本流が 0 件の日に、読むものが無いわけではない。**[09-09 の rejected.md](../2026-09-09/rejected.md) は、手動で lb=5 相当に広げて回収した論文のうち、**quota と時間の都合だけで落とした 6 件を id 付きで「繰り越し候補」として残している。**そこには **「hf 経路に乗っていないため、放置すると再配信されない」**と書いてある。

**本日 (4 日後) 確認した: 6 件すべて、ブリーフは作られておらず、以降の briefs から参照もされていない。**書いた本人が「放置すると消える」と警告した通りに消えた。

下をそのまま実行すると、6 件の abstract が `candidates_backlog.json` に落ちる。**次回の選別はこのファイルから始めればよい** (本日は arXiv API が 429 で叩けなかったため、id だけ渡す)。

```sh
cd ~/w/research_loop && python3 - <<'PY'
import json, urllib.request, urllib.parse, xml.etree.ElementTree as ET
NS = {"a": "http://www.w3.org/2005/Atom"}
ids = ["2609.05178","2609.05324","2609.04565","2609.04773","2609.04646","2609.04364"]
url = "http://export.arxiv.org/api/query?" + urllib.parse.urlencode(
    {"id_list": ",".join(ids), "max_results": "20"})
req = urllib.request.Request(url, headers={"User-Agent": "research-loop/1.0"})
root = ET.fromstring(urllib.request.urlopen(req, timeout=30).read())
out = [{"id": e.find("a:id", NS).text.split("/abs/")[-1],
        "title": " ".join(e.find("a:title", NS).text.split()),
        "abstract": " ".join(e.find("a:summary", NS).text.split()),
        "published": e.find("a:published", NS).text,
        "url": e.find("a:id", NS).text,
        "topic": "backlog_2026-09-09", "project": "?", "quota": 6}
       for e in root.findall("a:entry", NS)]
json.dump({"candidates": out}, open("candidates_backlog.json","w"),
          ensure_ascii=False, indent=2)
print(f"{len(out)} papers -> candidates_backlog.json")
PY
```

**6 件のうち、09-09 の自己評価で最上位だったのは `2609.05178` LIBERO-RECOVER** (「明日以降に回す価値が最も高い 1 本」)。**「タスクが成功したか」ではなく「失敗から復帰できるか」で評価軸を張る benchmark で、R1 が「衝突率の隣に置く量」として主張したい形と同型である。**429 が解けたら、まずこれ 1 本。

---

候補 **3 件** (planner_ai **0** / fm_distill **0** / next_arch **0** / wildcard 3) / 採用 **1 件** / 不採用 **2 件**
**上限 6 に対し 1。**空けた 5 枠は**候補が 1 件も存在しないため埋められない。基準を下げて埋めてはいない。**
**新規の候補は 3 件中 1 件だけで、それは採用した。不採用の 2 件はどちらも再配信であり、質で落としたものは 0 件である。**

> **【本日の fetch 診断】本流 3 トピックが揃って 0 件。ただし本日の意味は「また壊れた」ではない。**
> - **`lookback_days` の欠陥が、確率的な取りこぼしではなく曜日で決まる現象だと確定した** (土日月の 13 回すべて 0 件・反例なし)。**唯一の例外に見えた 09-09 (火) も、09-07 が米国 Labor Day で announce が 1 回飛んだためで、同じ規則で説明がつく。**
> - **欠陥#2 (dedup) の 8 回目。**`2608.12564` は**投稿から約 4 週間後の 3 回目の配信。**
> - **欠陥#2 の新しい型:** `2609.11929` は昨日は**本流 (fm_distill) 経路**、本日は**wildcard 経路**で届いた。wildcard 側の除外は**同じ実行の中で本流が拾った id しか見ていない**ため、日をまたぐと本流で読んだ論文が戻ってくる。
> - **欠陥#4 の追加事実:** wildcard 経路の `published` は arXiv ではなく **HF の `publishedAt`** で、`2608.12564` では**記録と 13 日矛盾している。修正④ を既存の `published` で実装すると、この論文は通ってしまう。**
> - **正直な記録: 本日は arXiv API が 4 回とも `HTTP 429` で、live の再クエリができていない。**診断は `candidates.json` の実測値と、repo の 6 週間分の記録に基づく。**曜日の規則性は単発クエリより強い証拠だが、「本日 arXiv が実際に何件返したか」は未確認である。**

---

## 今日読むべき TOP3 —— **本日はブリーフが 1 本しかないので、TOP1 + 読まずにできること 2 つ**

**新規の候補が 1 件しか届いていない日に TOP3 を 3 本並べるのは、順位ではなく水増しになる。**以下は本日実際に手を動かせる 3 つを、価値の順に並べたものである。

### 1. [NCP-ArchPreview — next-token prediction の上に「概念」の層を 1 枚足し、同じ loss を半分のトークンで出す](2609.10715.md) (P3 4.5 / P2 3.0)

**言語モデルは普通、次の 1 トークンを当てる学習 (NTP; next-token prediction) だけで事前学習する。**この論文は NTP を捨てずに、**その上に「複数トークンにまたがる離散的な単位」を当てる学習 (NCP; Next Concept Prediction) を 1 段重ねた。**8.9B パラメータを 5.73T トークンで学習し、**比較対象 (OLMo-3-7B) と同じ最終 loss に、トークンの 51.3% で到達した。**

**注目すべきは、その「概念」をどこから持ってきたかである。**外部の tokenizer も別の encoder も使わず、**モデル自身の hidden state を product quantization (ベクトルを小区間に分けて区間ごとに離散化する手法。小さな codebook の組み合わせで巨大な語彙を作れる) で離散化して語彙にした。**そして予測した概念をトークン側に戻して生成を誘導する。

**P3 にとっての含意は「抽象の層を足すが、細かい層は温存できる」という点にある。**運転の policy でも、1 ステップずつ行動を出す head を捨てるのは怖い —— 制御周期・安全層・既存の評価が全部そこに乗っている。**この論文は置き換えではなく追加として利得が出ることを 8.9B 規模で示し、しかも「構造」と「目標」のどちらが効いたかを切り分けている。**

> **学習を 1 回も回さずに 30 分で前提を検査できる。**この手法は「hidden state を離散化したコードが、複数ステップ持続し、数ステップ先まで予測できる」ことを前提にしている。**手元の軌跡予測モデルの hidden state に k-means を掛け、「1 つのコードが何フレーム続くか」の分布を出すだけでよい。フレームごとに切り替わるなら、そこに概念の構造は無く、この方向は運転では成立しない —— その時点で NO が出る。**negative result も収穫である (手順は[ブリーフ](2609.10715.md)に 4 ステップで書いた)。

### 2. 「貼るもの ①」の 2 行 —— **本日、根拠が「実測 4 回」から「曜日で決まる」に変わった**

**読む必要はない。**`topics.yaml` の 1 行と `fetch_candidates.py` の 1 行を書き換えるだけである。**21 日間「4 行・20 分」と書き続けてきたが、本日その 4 行が何を直すのかが閉じた形で分かった** —— `lookback_days` は投稿時刻に対して窓を切るが、論文が API に出てくるのは announce 後で 1〜4 日遅れる。**だから窓の手前側は構造的に空で、土日月は窓の幅が実質ゼロになる。**

**この 1 行で、上の TOP1 も変わる。**本日 P3 の論文を wildcard 経路から拾えたのは運がよかっただけで、**本流が動いていれば、hf に載らない論文から選べていた。**

### 3. 「貼るもの ②」の 6 件 —— **4 日前の自分が「放置すると消える」と書いて、その通り消えたもの**

**候補が 0 件の日に読むものが無い、という説明は本日使えない。**09-09 に id 付きで残した 6 件は、**4 日間ブリーフも参照もされていない。**うち `2609.05178` (LIBERO-RECOVER) は当時の自己評価で「回す価値が最も高い 1 本」である。**貼るもの ② のスクリプトは 3 分で 6 件の abstract を落とす。**

---

## 全ブリーフ

| id | タイトル | topic | project | スコア |
|---|---|---|---|---|
| [2609.10715](2609.10715.md) | NCP-ArchPreview — NTP の上に概念予測を 1 段重ねる latent-space LM | sns_wildcard | **P3** (+ P2) | P3 4.5 / P2 3.0 |

不採用 2 件の理由と fetch 診断 → **[rejected.md](rejected.md)**

---

## プロジェクト別の要点

### P1 (Planner AI + 評価) —— **候補 0 件。本日 P1 の材料は 1 つも無い**

**planner_ai の候補は 09-12 に続いて 2 日連続で 0 件。**ただし性質が違う:

- **09-12 の 0 件は keyword 不足だった** (arXiv は 30 件返していたが、17 日分にまたがり 1 日あたり約 1.8 件)。
- **本日の 0 件は窓の問題である** (どのトピックも 0 件。planner_ai 固有ではない)。

**この 2 つは別の欠陥で、別の修正 (③ と ①) が必要である。**本日の結果で修正③ (planner_ai への keyword 追加) の必要性が下がったわけではない —— **①を入れると、土日月以外の日に planner_ai の飢えが再び見えるようになる。**順序は変わらない: **① → ② → ③。**

**P1 に効く材料として本日示せるのは、繰り越しリストの 1 件だけである:** `2609.05178` LIBERO-RECOVER (P3 寄りだが、**「成功したか」から「失敗から復帰できるか」へ評価軸を移す benchmark** という主張の形が、R1 の solvability —— その瞬間に残っている逃げ道の本数 —— と同型)。**W37 の課題レーダーが挙げた「30 分で YES/NO が出る一歩」** ([2609.04921](../2026-09-09/2609.04921.md) を R1 の図 1 の代わりに引けるかの判定) **も、依然として未実施である。**

### P2 (Foundation Model 蒸留 + 適合) —— **候補 0 件。着想が 1 つ**

本流 0 件。本日のブリーフから持ち帰れるのは 1 点だけで、**裏付けではなく着想である。**

[NCP-ArchPreview](2609.10715.md) が product quantization で作った概念コードは、**teacher と student の間で照合する単位の候補になる。**09-11 の [OnPoKD](../2026-09-11/2609.10321.md) は**教師信号の中身**を、09-12 の [per-token gating](../2026-09-12/2609.11768.md) は**信号との距離の測り方**を、どちらも**トークン単位**で変える設計だった。**概念単位で照合すれば、目標は粗くなる代わりに分散が下がる。**ただし本論文は蒸留を一切やっていないので、これは仮説の位置にとどまる。

**繰り越しリストの P2 3 件のほうが、本日のブリーフより P2 には直接効く** —— とくに `2609.04565` (**on-policy distillation で監督を全体の 0.05% に絞っても full-token 学習に匹敵**) は、09-09 の記録で「R2 の軸『支配変数は監督を当てる範囲』の最も直接的な検証例」と評価されている。

### P3 (次世代アーキテクチャ) —— 「抽象の層をどこから調達するか」で 3 本が並んだ

本日の 1 本を過去 2 日と並べると、軸がはっきりする。**3 本すべて「生成モデルの上に明示的な抽象の層を置く」話で、その層の調達方法で分かれている:**

| 論文 | 抽象の層の正体 | 調達方法 |
|---|---|---|
| [Programmable World Model](../2026-09-11/2609.10540.md) (09-11) | entity state と遷移規則 | **人が書く** (プログラム。学習は入らない) |
| [NCP-ArchPreview](2609.10715.md) (本日) | 複数トークンにまたがる離散概念 | **モデル自身の hidden state から PQ で学習** |
| [IMLE-VLA](../2026-09-12/2609.10915.md) (09-12) | (層を足さず、生成を 1 step にする) | —— |

**PWM は「決定的に書けるものは書け」、NCP は「書かずに自分の内部状態から取れ」。**運転には両方の材料がある (交通規則は書ける / 他車の意図は書きにくい) ので、**選択問題ではなく「どこまでを書き、どこからを学習された離散コードに任せるか」という境界の設計問題になる。**この境界を 1 枚の図にすると R3 のマップの骨格になるが、**R3 は今月は着手しない方針である。**

**なお本日、hf_upvotes のタイブレークは発動しなかった** (新規候補が 1 件だけで、同点の比較が起きていない)。09-11 に記録した欠陥#5 (hf のタイブレークは negative result を構造的に不利にする) は、本日は結果に影響していない。**ただし別の形で hf 依存が出た: 本日ブリーフにできた 1 本は hf_upvotes 209 の wildcard 経路の論文で、本流が 0 件だったため「hf に載った論文しか読めない日」になっている。**これは選別の問題ではなく、fetch の問題である。
